Name: todo-api
Host: 0.0.0.0
Port: 8888
Debug: false

# JWT 配置（AccessSecret 首次启动时自动生成并持久化到数据库，此值为 fallback）
Auth:
  AccessSecret: "todo-app-jwt-secret-key-2024"
  AccessExpire: 86400

# 数据库配置（运行时注入）
Database:
  DataDir: "data"
  DBFile: "todo.db"